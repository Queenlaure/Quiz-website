let btn = document.getElementById('btn')
   function showAlert() {
       let myText = "form has been submitted successfully";
       alert(myText);
}

btn.addEventListener('click', () => showAlert())
